model QuantityReportDetail {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @map("id") @db.Uuid
  quantityReportId String?   @map("quantity_report_id") @db.Uuid
  storageQuantity  Float?    @map("storage_quantity")
  recordedQuantity Float?    @map("recorded_quantity")
  createdAt        DateTime? @map("created_at") @db.Timestamptz(6)
  updatedAt        DateTime? @map("updated_at") @db.Timestamptz(6)
  deletedAt        DateTime? @map("deleted_at") @db.Timestamptz(6)

  quantityReport QuantityReport? @relation(fields: [quantityReportId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("quantity_report_detail")
}
